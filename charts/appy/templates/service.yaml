apiVersion: v1
kind: Service
metadata:
  name: {{ include "appy.fullname" . }}
  labels:
    {{- include "appy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appy.selectorLabels" . | nindent 4 }}
