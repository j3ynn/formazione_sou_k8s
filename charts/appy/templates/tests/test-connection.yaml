apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appy.fullname" . }}-test-connection"
  labels:
    {{- include "appy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
